[Unit]
Description=Jarvis message ingest worker
After=network.target

[Service]
Type=oneshot
WorkingDirectory=${MOLT_EVO_WORKSPACE:-/root/.openclaw/workspace}
ExecStart=/bin/bash -lc 'python3 ${MOLT_EVO_WORKSPACE:-/root/.openclaw/workspace}/jarvis_message_autoload.py'
