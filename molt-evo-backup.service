[Unit]
Description=Jarvis local memory backup
After=network.target

[Service]
Type=oneshot
WorkingDirectory=${MOLT_EVO_WORKSPACE:-/root/.openclaw/workspace}
ExecStart=/bin/bash ${MOLT_EVO_WORKSPACE:-/root/.openclaw/workspace}/jarvis_backup.sh
